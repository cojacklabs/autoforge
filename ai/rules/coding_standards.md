# Coding Standards

- TypeScript: no errors (`tsc --noEmit` must pass).
- ESLint: zero warnings (or repo policy); fixable issues should be auto‑fixed where allowed.
- Prettier: format changes allowed per policy; check must pass.
- JSON: strict JSON; trailing commas disallowed; validate with schema where applicable.
- YAML: use spaces (no tabs); avoid anchors unless necessary; validate for syntax.
- Markdown: proper headings, fenced code blocks, lint if configured.
- OpenAPI: lint contracts if present.
