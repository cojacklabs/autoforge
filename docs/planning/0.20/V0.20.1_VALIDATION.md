# v0.20.1 Validation Checkpoint

- Versioned project metadata supports lifecycle, aliases, runtime, governance,
  capabilities, freshness, and retention policy fields.
- `projects show <path> [--json]` exposes project metadata.
- `projects list --json` exposes machine-readable project inventory.
- `projects storage <path> [--json]` reports usage, lifecycle, and retention.
- `projects archive` and `projects restore` provide reversible lifecycle control.
- `projects prune --dry-run` previews inaccessible entries without mutation.
- Legacy registry metadata normalizes safely with active lifecycle defaults.
- Focused project metadata and workspace tests pass.

Next: complete the v0.20.1 release-readiness audit.
