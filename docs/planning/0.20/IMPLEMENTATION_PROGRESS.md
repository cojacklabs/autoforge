# v0.20 Implementation Progress

- Product Digital Twin architecture decisions are recorded.
- Versioned twin schemas and bounded query contracts are implemented.
- Deterministic projections derive from work and decision state.
- Project-local projection persistence uses atomic writes.
- `autoforge twin generate`, `twin show`, and `twin query` are available.
- CLI integration coverage and documentation are updated.

Next: complete the v0.20 release-readiness audit before versioning.
