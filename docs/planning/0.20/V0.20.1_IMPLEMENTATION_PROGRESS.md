# v0.20.1 Implementation Progress

- Project metadata schema and compatibility migration are implemented.
- Project inspection, JSON inventory, lifecycle controls, and storage
  diagnostics are implemented.
- Archive retention metadata and safe prune preview are implemented.
- CLI help, reference documentation, and focused tests are synchronized.

Next: audit v0.20.1 release readiness before versioning.
